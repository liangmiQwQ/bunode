# Repository Guidance

- Keep the project under `/Users/liangmi/code/liangmiQwQ/bunode`.
- Use `pnpm-workspace.yaml` for workspace and pnpm settings.
- Use Vite+ tasks through `vp` / `vp run c...`; do not add a `justfile`.
- Use `@liangmi/vp-config` for JavaScript and TypeScript tooling.
- Keep the Rust workspace layout aligned with `liangmiQwQ/rs-starter`.
